# Why: central defaults for local docker runtime.
# Scope: all services in docker-compose use this baseline.
# Verify: containers start with correct ports and model IDs.
TEXT_MODEL=deepseek-v3.2
VISION_MODEL=qwen3-vl-flash
REDIS_URL=redis://redis:6379/0
